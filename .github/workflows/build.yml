name: Build Haxe & Haxelib ARM64

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Forked Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup OCaml Environment
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: 5.2.x

      - name: Install System Dependencies & Cross-Compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-aarch64-linux-gnu \
            g++-aarch64-linux-gnu \
            make \
            libpcre2-dev \
            zlib1g-dev \
            libmbedtls-dev \
            pkg-config \
            neko \
            neko-dev

      - name: Install OPAM Dependencies
        run: |
          eval $(opam env)
          opam install . --deps-only --with-test -y

      - name: Build Haxe & Haxelib Binaries for ARM64
        run: |
          eval $(opam env)
          make all CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ static=1

      - name: Upload Binaries Artifact
        uses: actions/upload-artifact@v4
        with:
          name: haxe-arm64-full
          path: |
            haxe
            haxelib
